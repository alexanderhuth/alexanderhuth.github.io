source "https://rubygems.org"
gem 'github-pages', group: :jekyll_plugins

# Pin a newer minitest to avoid Ruby 3.3 incompatibility with older releases
# (some dependencies resolve to minitest 5.14.x which requires Ruby < 3.1).
gem 'minitest', '>= 5.20'